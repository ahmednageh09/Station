export class Station {
  stationName!: string;
  totalSales!: number;
  comoressionState!: string;
  maxTransactionHour!: number;
  totalValue!: number;
  totalTransactionCount!: number;
  maxTransaxtionDate!: string;
}
